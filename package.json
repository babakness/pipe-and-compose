{
  "name": "pipe-and-compose",
  "description": "Implementation of `pipe` and `compose` using types from at 'pipe-and-compose-types.",
  "version": "0.0.6",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run test && npm run build",
    "test": "ts-node src/index.test.ts",
    "dev": "tsc -w",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^11.13.2",
    "ts-node": "^8.0.3",
    "tsc": "^1.20150623.0",
    "typescript": "^3.4.2"
  },
  "dependencies": {
    "pipe-and-compose-types": "^0.0.11",
    "typescript-tuple": "^2.2.1"
  }
}
